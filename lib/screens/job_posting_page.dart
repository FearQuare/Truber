import 'package:flutter/material.dart';

class JobPostingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Job Postings'),
      ),
      body: Center(
        child: Text('List of job postings will be here'),
      ),
    );
  }
}